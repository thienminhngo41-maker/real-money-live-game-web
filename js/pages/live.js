export const streams=[{id:'LIVE 01',url:'https://vdo.ninja/?view=UgMJMrr'},{id:'LIVE 02',url:'https://vdo.ninja/?view=PHhrWPR'}];
export function renderLive(t){return `<section id="page-live" class="page"><div class="page-title">${t.live}</div><div class="live-note">${t.liveNote}</div>${streams.map(s=>`<div class="live-card" data-stream="${s.url}" data-label="${s.id}" role="button" tabindex="0" aria-label="${s.id}"><span class="live-card-label">${s.id}</span><iframe src="${s.url}&autoplay=1&muted=1" allow="autoplay; fullscreen; picture-in-picture; encrypted-media" allowfullscreen loading="eager"></iframe><div class="live-overlay"></div></div>`).join('')}</section>`}
